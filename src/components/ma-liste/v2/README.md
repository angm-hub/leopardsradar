# Ma Liste v2 — composants nouvelle DA

Cf. `docs/DESIGN_MA_LISTE_V2.md` pour la direction artistique.

## Composants à livrer (Sprint 3)

- `MaListeV2.tsx` — page racine, 1 seul écran
- `Pitch.tsx` — SVG terrain RDC + slots + drag handling
- `Library.tsx` — sticky sidebar/bottom sheet, search ⌘K
- `TopBar.tsx` — logo + formation toggle pills + save indicator
- `BenchStrip.tsx` — 15 slots banc + bench
- `StickyShareCTA.tsx` — bottom CTA sticky avec progress
- `ToastUndo.tsx` — toast undo pour actions destructives
- `ShareModalV2.tsx` — modal share (pseudo input + 3 actions)

Tous mobilisent : `taste-skill` + `senior-ui-engineer` + `impeccable shape/craft`.
Copy passé par `kaira-identite` + `humanize-text`.

## Status
🟡 Scaffold uniquement (Sprint 2 partiel). Sprint 3 = implémentation.
